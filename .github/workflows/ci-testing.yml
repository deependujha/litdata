name: CI Testing

# see: https://help.github.com/en/actions/reference/events-that-trigger-workflows
on: # Trigger the workflow on push or pull request, but only for the main branch
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, reopened, ready_for_review, synchronize]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    shell: bash

jobs:
  torch-support:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      latest: ${{ steps.policy.outputs.latest }}
      previous: ${{ steps.policy.outputs.previous }}
      minimum: ${{ steps.policy.outputs.minimum }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Validate the PyTorch support policy
        run: python3 .github/scripts/torch_support.py check
      - name: Export the supported PyTorch versions
        id: policy
        run: python3 .github/scripts/torch_support.py export

  pytester:
    needs: torch-support
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-22.04", "macos-14", "windows-2022"]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        exclude:
          - { os: "windows-2022", python-version: "3.13" }
          - { os: "windows-2022", python-version: "3.14" }
        # every combination installs the newest PyTorch; this entry merges into the existing
        # ubuntu-22.04 / 3.12 job (it adds no job) so the previous supported minor is covered too
        include:
          - { os: "ubuntu-22.04", python-version: "3.12", torch-version: "${{ needs.torch-support.outputs.previous }}" }

    # Timeout: https://stackoverflow.com/a/59076067/4521646
    timeout-minutes: 60
    env:
      UV_TORCH_BACKEND: "cpu"
      # Supply-chain guard: skip PyPI releases newer than this. See https://docs.astral.sh/uv/reference/settings/#exclude-newer
      UV_EXCLUDE_NEWER: "2 days"

    steps:
      # FFmpeg is required for Video Deserializer tests
      - name: Setup FFmpeg (shared libs, Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Setup FFmpeg (shared libs, macOS)
        if: runner.os == 'macOS'
        run: brew install ffmpeg

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install uv and setup python ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          activate-environment: true
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Install package & dependencies
        env:
          # empty for the newest release, `torch==X.Y.*` on the previous-minor job
          TORCH_PIN: ${{ matrix.torch-version && format('torch=={0}.*', matrix.torch-version) || '' }}
        run: |
          uv pip install -U lightning-sdk
          uv pip install -e ".[extras]" -r requirements/test.txt ${TORCH_PIN} -U -q
          uv pip list
          python -c "import torch; print('torch', torch.__version__)"

      - name: Run fast tests in parallel
        run: |
          pytest tests \
              --ignore=tests/processing \
              --ignore=tests/raw \
              -n 2 --dist=loadgroup --cov=litdata --durations=0 --timeout=120 --capture=no --verbose

      - name: Run processing tests sequentially
        run: |
          # note that the listed test should match ignored in the previous step
          pytest \
            tests/processing tests/raw \
            --cov=litdata --cov-append --durations=0 --timeout=120 --capture=no --verbose

      - name: Statistics
        continue-on-error: true
        run: |
          coverage report
          coverage xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ./coverage.xml
          flags: unittests,${{ matrix.os }},${{ matrix.python-version }}
          env_vars: OS,PYTHON
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: Minimize uv cache
        run: uv cache prune --ci

  testing-guardian:
    runs-on: ubuntu-latest
    needs: pytester
    if: always()
    steps:
      - run: echo "${{ needs.pytester.result }}"
      - name: failing...
        if: needs.pytester.result == 'failure'
        run: exit 1
      - name: cancelled or skipped...
        if: contains(fromJSON('["cancelled", "skipped"]'), needs.pytester.result)
        timeout-minutes: 1
        run: sleep 90
